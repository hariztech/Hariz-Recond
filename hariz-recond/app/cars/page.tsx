import CarCard from '../../components/CarCard'; import {cars} from '../../data/cars'
export default function Cars(){return <main className="container-x py-16"><p className="text-xs font-bold uppercase tracking-[.25em] text-gold">Inventory</p><div className="mt-2 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><h1 className="text-4xl font-black sm:text-5xl">Find your next car.</h1><span className="text-sm text-white/50">{cars.length} vehicles available</span></div><div className="mt-10 grid gap-6 md:grid-cols-2 lg:grid-cols-3">{cars.map(c=><CarCard key={c.id} car={c}/>)}</div></main>}
