import Link from 'next/link'; import Image from 'next/image'; import {Car} from '../data/cars'
export default function CarCard({car}:{car:Car}){return <article className="group overflow-hidden rounded-2xl border border-white/10 bg-white/[.035] transition hover:-translate-y-1 hover:border-gold/40 hover:shadow-gold"><div className="relative aspect-[16/10] overflow-hidden"><Image src={car.image} alt={car.name} fill className="object-cover transition duration-700 group-hover:scale-105" sizes="(max-width:768px) 100vw, 33vw"/><div className="absolute inset-0 bg-gradient-to-t from-black/70 to-transparent"/>{car.badge&&<span className="absolute left-4 top-4 rounded-full border border-gold/40 bg-black/70 px-3 py-1 text-xs font-bold text-gold">{car.badge}</span>}</div><div className="p-5"><div className="flex items-start justify-between gap-4"><div><p className="text-xs font-bold uppercase tracking-widest text-gold">{car.year} · Grade {car.grade}</p><h3 className="mt-1 text-lg font-bold">{car.name}</h3></div><p className="whitespace-nowrap text-lg font-black text-gold">RM {car.price.toLocaleString()}</p></div><div className="mt-4 grid grid-cols-3 gap-2 border-y border-white/10 py-3 text-xs text-white/60"><span>{car.mileage}</span><span>{car.transmission}</span><span>{car.fuel}</span></div><Link href={`/cars/${car.id}`} className="mt-4 block rounded-xl border border-white/15 px-4 py-3 text-center text-sm font-bold transition hover:border-gold hover:text-gold">View Details</Link></div></article>}
